import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'invoice-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
